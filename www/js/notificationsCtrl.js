/*
angular.module('app.notificationsCtrl', ['ngSanitize', 'ngAnimate'])
.controller('notificationsCtrl', ['$scope', 'addAlert', 'openSocketService', '$stateParams', '$http', '$state', '$location', '$templateCache', '$rootScope',
      function($scope, addAlert, openSocketService, $stateParams, $http, $state, $location, $templateCache, $rootScope) {
      console.warn('notifications: ',$scope.base_msg_data);
      $scope.removeMessage = function(msg) {
            console.log("message to remove: " + msg);
      }
}]);
*/
